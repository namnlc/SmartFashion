import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imgFlags: {
    width: 25,
    height: 25,
  },
  selected: {
    borderRadius: 25,
    borderWidth: 0.5,
    borderColor: 'blue',
  },
});
